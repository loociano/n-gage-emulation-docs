theme: jekyll-theme-minimal
collections:
  tutorials:
    output: true
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"